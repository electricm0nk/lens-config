# generated: 2026-03-30
# project: terminus-infra-crossplane
# project_key: NOKEY
# tracking_system: file-system
# story_location: docs/implementation-artifacts
#
# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - TECH-CHANGE TRACK: Epics derived from architecture.md + adversarial-review-report.md
# - No devproposal phase — sprint planning starts directly from architecture
# - Version-pinning (Story 1.1) is a pre-implementation BLOCKER — must be completed first
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories must be executed in dependency order (see epics.md dependency map)
# - Dev moves story to 'review', then runs code-review (fresh context)

generated: 2026-03-30
project: terminus-infra-crossplane
project_key: NOKEY
tracking_system: file-system
story_location: docs/implementation-artifacts

development_status:
  # ── Epic 1: Crossplane Core Installation ──────────────────────
  epic-1: done
  1-1-research-and-pin-crossplane-provider-versions: done
  1-2-create-crossplane-core-argocd-application: done
  1-3-create-crossplane-core-values-yaml: done
  1-4-create-kustomization-yaml-app-of-apps-registration: done
  epic-1-retrospective: optional
  epic-1-pr: "https://github.com/electricm0nk/terminus.infra/pull/58"

  # ── Epic 2: Provider Installation ─────────────────────────────
  epic-2: done
  2-1-create-provider-kubernetes-yaml: done
  2-2-create-provider-helm-yaml: done
  2-3-validate-injectedidentity-rbac-for-provider-kubernetes: done
  epic-2-retrospective: optional
  epic-2-pr: "https://github.com/electricm0nk/terminus.infra/pull/59"

  # ── Epic 3: ProviderConfig, Sync Orchestration, and Acceptance ─
  epic-3: done
  3-1-create-kubernetes-default-providerconfig: done
  3-2-create-helm-default-providerconfig: done
  3-3-configure-argocd-sync-wave-ordering: done
  3-4-post-install-acceptance-testing: done
  epic-3-retrospective: optional
  epic-3-pr: "https://github.com/electricm0nk/terminus.infra/pull/60"

  # ── Batch Run Metadata ─────────────────────────────────────────
  batch_run_date: "2026-03-29"
  batch_run_by: "Claude Sonnet 4.6 (@lens dev batch)"
  open_items_for_review:
    - id: RBAC-SA-name
      story: "2.3"
      severity: high
      description: >
        rbac-provider-kubernetes.yaml subject SA name is a placeholder.
        The actual Crossplane-generated SA name includes a hash suffix.
        Manifest will not bind correctly until updated post-install.
        See runbook.md §Post-Install RBAC for the discovery command.
    - id: kustomization-story-ac-delta
      story: "1.4"
      severity: low
      description: >
        Story 1.4 AC specified namespace: crossplane-system in kustomization.yaml.
        This was intentionally omitted — setting namespace would override argocd ns
        on the nested Application object and break it. Correction documented in PR #58.
    - id: story-1-2-single-vs-multi-source
      story: "1.2"
      severity: low
      description: >
        Story 1.2 AC specified single-source Application. Implemented as multi-source
        to follow existing repo pattern (cert-manager, external-secrets) and properly
        reference values.yaml from git. Documented in PR #58.
