# generated: 2026-03-28
# project: terminus-infra-semaphoreui
# project_key: NOKEY
# tracking_system: file-system
# story_location: docs/terminus/infra/semaphoreui/stories

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories should be worked in dependency order (Epic 1 before Epic 2; Epic 3 is independent)
# - Story 1.1 and 1.2 are prerequisites for Story 2.1 (DB + secrets must exist before pod start)
# - Epic 3 is independent of Epic 2 but can be worked concurrently
# - SM typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: "2026-03-28"
project: terminus-infra-semaphoreui
project_key: NOKEY
tracking_system: file-system
story_location: docs/terminus/infra/semaphoreui/stories

development_status:
  # --- Epic 1: Data Foundation (Postgres + Vault Secrets) ---
  epic-1: done
  1-1-provision-semaphore-postgres-db: done
  1-2-seed-vault-eso-externalsecret: done
  epic-1-retrospective: optional

  # --- Epic 2: Semaphore Application Delivery ---
  # Prerequisite: Epic 1 complete (DB + secrets must exist before pod scheduling)
  epic-2: done
  2-1-semaphore-deployment-service-pvc: done
  2-2-semaphore-ingress-certificate: done
  2-3-register-semaphore-argocd-app: done
  epic-2-retrospective: optional

  # --- Epic 3: ArgoCD Platform Access ---
  # Independent of Epic 2; can be worked concurrently with Epic 1 or 2
  epic-3: done
  3-1-argocd-insecure-mode-ingress: done
  epic-3-retrospective: optional
